<main class="user-wrapper">
  <div class="post-new-form-body">
    <div class="post-new-form-lists">
      <%= form_with model: [@user], local: true do |f| %>
      <%= render "devise/shared/error_messages", model: f.object %>

        <div class="form-field">
          <label class="form-text">ニックネーム</label>
          <%= f.text_field :nickname, placeholder:"ニックネーム (20文字以内)", maxlength:"20", class:"form-default" %>
        </div>

        <div class="form-field">
          <label class="form-text">ユーザー画像</label>
            <%= f.file_field :image %>
        </div>

        <div class="post-form-btn">
          <%= f.submit "作成", class:"form-blue-btn" %>
        </div>

      <% end %>
    </div>
  </div>
</main>